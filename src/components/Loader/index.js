import React from 'react';
import { MainWrapper, LoaderWrapper, LoaderContent } from './styled';

const Loader = () => (
  <MainWrapper>
    <LoaderWrapper>
      <LoaderContent></LoaderContent>
    </LoaderWrapper>
  </MainWrapper>
);

export default Loader;
